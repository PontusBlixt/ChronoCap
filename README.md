# ChronoCap
Chronological PCAP aggregator. Takes two sets of packet captures — typically from two different network interfaces or capture points — and merges them into a single, strictly time-ordered output fileset
